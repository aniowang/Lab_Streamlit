{
    "name": "Lab_Streamlit",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "args": { }
    },
    "runArgs": [
        "-u", "vscode"
    ],
    "settings": { 
        // 设置 VS Code 的各种配置
    },
    "extensions": [
        // 安装的 VS Code 扩展列表
    ],
    "postCreateCommand": "pip install plotly",
    "forwardPorts": [],
    "remoteUser": "vscode"
}
